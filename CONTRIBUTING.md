# 动作素材协作说明

## 推荐分工

- 动作制作同事：生成或剪辑原始动作，保证人物、镜头、光线和基准画面一致。
- 素材整合同事：使用导入工具统一编码，填写动作状态和动作窗口。
- 前端验收同事：在四种目标尺寸检查构图、语音圆、衔接、性能和状态优先级。

## 分支与提交

每个动作或同一组动作使用独立分支，例如：

```text
action/listening-focus
action/speaking-soft
action/emotion-smile
```

一次提交只加入一组相关动作。提交信息建议使用：

```text
add listening focus actions
add soft speaking loop
refine blink timing
```

Pull Request 需要写明：

- 动作所属状态和触发场景。
- 原始时长、处理后时长、帧率和分辨率。
- 动作真实发生的时间范围。
- 是否与基准图首尾一致。
- 四种尺寸是否验收通过。
- 是否运行了 `python tools/yeshuang_scene_pack.py validate`。

## 禁止提交

- 未授权人物、音乐或声线素材。
- 带平台水印、字幕、黑边、黑框或提示词残留的视频。
- 超过 25 MB 的单个动作成片。
- 模型文件、语音参考文件、聊天记录、日记、令牌、密钥、运行日志或个人路径。
- 只在某个窗口尺寸对齐、依赖固定像素位置的界面改动。

## 合并前清单

- [ ] 视频为 `1536×1024`、H.264、`yuv420p`、24 或 30 FPS、无音轨。
- [ ] 视频开头和结尾都能自然回到统一基准画面。
- [ ] 没有镜头推进、缩放、抖动、突然变脸或 Q 版闪帧。
- [ ] 普通眨眼闭合到睁开约 `0.18–0.35s`；若是慵懒合眼或浅睡动作，则保留有意设计的慢节奏并登记为 `drowsy`/`sleepy`，不能伪装成普通 `blink`。
- [ ] `manifest.json` 中 ID 唯一、状态正确、路径有效。
- [ ] 动作切换没有硬切、黑闪、尺寸跳动或双层同时播放。
- [ ] 语音圆在四种目标尺寸仍与画面圆心重合。
- [ ] 校验工具通过。
